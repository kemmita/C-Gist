#include <stdio.h>
#include <stdlib.h>

struct Student{
    int age;
    double gpa;
};

int main() {
    struct Student s1, s2, s3;
    s1.age = 24;
    s1.gpa = 3.4;
    printf("The student is  %d years old and holds a GPA of %.1f",s1.age, s1.gpa);
    return 0;
}
